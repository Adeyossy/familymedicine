// This serverless function gets information about the login state

import { HandlerContext, HandlerEvent } from "@netlify/functions";

const authHandler = (event: HandlerEvent, context: HandlerContext) => {
}